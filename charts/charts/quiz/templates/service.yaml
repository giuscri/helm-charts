apiVersion: v1
kind: Service
metadata:
  name: {{ include "quiz.fullname" . }}
  labels:
    {{- include "quiz.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "quiz.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "quiz.fullname" . }}-test-connection"
  labels:
    {{- include "quiz.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "quiz.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
